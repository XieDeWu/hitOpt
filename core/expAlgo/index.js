/* ══════════════════════════════════════════════════════════════════════════════════════
 * Horae v6.39.1 —— **实验算法清单**（`core/expAlgo/index.js`）
 *
 *   用户 2026-09-19 定版："每个算法必须独立代码块，建议单独的js文件"
 *
 * ── 这个文件只干一件事：把本目录下每个算法文件"跑一遍"────────────────────────────
 *   每个算法文件在被 import 的那一刻，就会自己调 `register({...})` 把名字/版本/本体登记进去
 *   （副作用导入）。所以加算法**只需要在这里多写一行 import**。
 *
 * ── 顺序 ────────────────────────────────────────────────────────────────────────
 *   下拉框按注册表里的 `id` 排序（`list()` 里排过），**与这里的 import 顺序无关**
 *   ⇒ 不会因为谁先加载就换位置。
 * ══════════════════════════════════════════════════════════════════════════════════════ */
import './mainAlgo.js';        // ⓪ **主算法也是算法**（用户 2026-09-19 定版）—— 产物在盘上，kind='disk'
import './ptrExact.js';        // ① 精确匹配指针化 v1（壳只有序号 ⇒ 不可还原、会重号；留着当对照）
import './ptrExactV2.js';      // ② 精确匹配指针化（可还原）v2 —— 用户 2026-09-19："复制 新建V2算法"
import './ptrExactV2_2.js';    // ③ 连续行去重（可还原）v2.2 —— 用户 2026-09-19："复制新建v2.2算法 重点是解决重复问题"
import './ptrExactV2_3.js';    // ④ 最大公共子串 v2.3 —— 用户 2026-09-19 看完 v2.2 产物后："不行。你必须按A B 两者的最大公共子串算法（至少300字公共长）"
import './ptrExactV2_4.js';    // ⑤ 最长公共子串（兼容变量区）v2.4 —— 用户 2026-09-19："复制 新增v2.4算法" ＋ "希望能兼容变量区把"
import './amBlockPtr.js';       // ⑥ **块级指针化** v1 —— 用户 2026-09-20："算法组合管线"（原来写死在主链路里的第一层）
import './amAddrRecall.js';     // ⑦ **区间唤起** v1 —— 同上（原来写死在主链路里的第二层）

export { ALGOS, register, list, get, label } from './registry.js';
export { MAIN_ID } from './mainAlgo.js';
